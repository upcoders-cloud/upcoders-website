import en from '@/i18n/translations/en.js'
import pl from '@/i18n/translations/pl.js'

export const TRANSLATIONS = {
  en,
  pl,
}

export const SUPPORTED_LANGUAGES = Object.keys(TRANSLATIONS)
export const DEFAULT_LANGUAGE = 'en'
